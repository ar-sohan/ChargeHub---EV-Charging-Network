# PostgreSQL is configured in src/app.module.ts in the current project.
# Add your SMTP credentials to a local .env file. Do not commit that file.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password
MAIL_FROM="ChargeHub EV <your-email@gmail.com>"
